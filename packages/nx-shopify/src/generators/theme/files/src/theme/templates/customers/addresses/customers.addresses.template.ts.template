import {
  ThemeModule,
  ThemeContext,
  ThemeOnReady,
} from '<%= importPath %>/core';

import './customers.addresses.template.scss';

export class CustomersAddressesTemplate extends ThemeModule implements ThemeOnReady {
  constructor(context: ThemeContext) {
    super(context);
  }

  onReady() {
    console.log('CustomersAddresses Template: onReady() called');
  }
}

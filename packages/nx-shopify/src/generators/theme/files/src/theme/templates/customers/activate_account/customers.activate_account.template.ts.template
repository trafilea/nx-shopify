import {
  ThemeModule,
  ThemeContext,
  ThemeOnReady,
} from '<%= importPath %>/core';

import './customers.activate_account.template.scss';

export class CustomersActivateAccountTemplate extends ThemeModule implements ThemeOnReady {
  constructor(context: ThemeContext) {
    super(context);
  }

  onReady() {
    console.log('CustomersActivateAccount Template: onReady() called');
  }
}

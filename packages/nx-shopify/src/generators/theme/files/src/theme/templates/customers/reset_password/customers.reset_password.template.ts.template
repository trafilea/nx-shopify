import {
  ThemeModule,
  ThemeContext,
  ThemeOnReady,
} from '<%= importPath %>/core';

import './customers.reset_password.template.scss';

export class CustomersResetPasswordTemplate extends ThemeModule implements ThemeOnReady {
  constructor(context: ThemeContext) {
    super(context);
  }

  onReady() {
    console.log('CustomersResetPassword Template: onReady() called');
  }
}
